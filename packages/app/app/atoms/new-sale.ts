import { atom } from "jotai";

// UI State - Local component state for variant selector modal
export const showVariantSelectorAtom = atom(false);

// UI State - Calculator modal (drawer)
export const showCalculatorModalAtom = atom(false);
