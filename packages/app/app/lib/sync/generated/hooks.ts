// AUTO-GENERATED FILE - DO NOT EDIT
// Generated by drizzle-sync from backend schema
// Engine-first hooks using useEngineService + TanStack Query

import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { useEngineService } from "@avileo/drizzle-sync/react";
import { useState } from "react";
import { createId } from "@paralleldrive/cuid2";
import { getFileUploadService } from "@avileo/drizzle-sync/client";
import {
  AbonosService,
  CustomerGroupsService,
  CustomersService,
  DistribucionesService,
  FilesService,
  ProductVariantsService,
  ProductsService,
  PurchasesService,
  SalesService,
  SuppliersService,
  TagsService,
  VisitasService,
} from "./services";
import type {
  CreateAbonosInput,
  CreateCustomerGroupsInput,
  CreateCustomersInput,
  CreateDistribucionesInput,
  CreateFilesInput,
  CreateProductVariantsInput,
  CreateProductsInput,
  CreatePurchasesInput,
  CreateSalesInput,
  CreateSuppliersInput,
  CreateTagsInput,
  CreateVisitasInput,
  UpdateAbonosInput,
  UpdateCustomerGroupsInput,
  UpdateCustomersInput,
  UpdateDistribucionesInput,
  UpdateFilesInput,
  UpdateProductVariantsInput,
  UpdateProductsInput,
  UpdatePurchasesInput,
  UpdateSalesInput,
  UpdateSuppliersInput,
  UpdateTagsInput,
  UpdateVisitasInput,
} from "./services";

// Customers hooks
export interface CustomersListOptions {
  search?: string;
  limit?: number;
  offset?: number;
  sortBy?: "createdAt" | "updatedAt";
  sortOrder?: "asc" | "desc";
}

export function useCustomerss(options?: CustomersListOptions) {
  const service = useEngineService<CustomersService>("customers");

  return useQuery({
    queryKey: options ? ["customers", "list", options] : ["customers"],
    queryFn: async () => service.list(options),
  });
}

export function useCustomers(id: string | null) {
  const service = useEngineService<CustomersService>("customers");

  return useQuery({
    queryKey: id ? ["customers", id] : ["customers", "detail"],
    queryFn: async () => {
      if (!id) return null;
      return service.findById(id);
    },
    enabled: !!id,
  });
}

export function useCreateCustomers() {
  const service = useEngineService<CustomersService>("customers");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (inputParam: CreateCustomersInput) => {
      let input = inputParam;

      return service.create(input);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["customers"] });
    },
  });
}

export function useUpdateCustomers() {
  const service = useEngineService<CustomersService>("customers");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async ({ id, input: inputParam }: { id: string; input: UpdateCustomersInput }) => {
      let input = inputParam;

      return service.update(id, input);
    },
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ["customers"] });
      queryClient.invalidateQueries({ queryKey: ["customers", variables.id] });
    },
  });
}

export function useDeleteCustomers() {
  const service = useEngineService<CustomersService>("customers");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (id: string) => {
      return service.delete(id);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["customers"] });
    },
  });
}

// Products hooks
export interface ProductsListOptions {
  search?: string;
  limit?: number;
  offset?: number;
  sortBy?: "createdAt" | "updatedAt";
  sortOrder?: "asc" | "desc";
}

export function useProductss(options?: ProductsListOptions) {
  const service = useEngineService<ProductsService>("products");

  return useQuery({
    queryKey: options ? ["products", "list", options] : ["products"],
    queryFn: async () => service.list(options),
  });
}

export function useProducts(id: string | null) {
  const service = useEngineService<ProductsService>("products");

  return useQuery({
    queryKey: id ? ["products", id] : ["products", "detail"],
    queryFn: async () => {
      if (!id) return null;
      return service.findById(id);
    },
    enabled: !!id,
  });
}

export function useCreateProducts() {
  const service = useEngineService<ProductsService>("products");
  const queryClient = useQueryClient();
  const [fileUploadState, setFileUploadState] = useState<
    Record<
      string,
      { status: "idle" | "uploading" | "pending" | "done" | "error"; progress: number }
    >
  >({});

  return useMutation({
    mutationFn: async (inputParam: CreateProductsInput) => {
      let input = inputParam;
      // Process file fields
      const fileService = getFileUploadService();
      let processedInput = { ...input };
      if ((input.imageId as any) instanceof File) {
        const fileId = createId();
        const file = input.imageId as unknown as File;
        await fileService.saveTemp(fileId, file, {
          entityType: "assets",
          fieldName: "imageId",
          filename: file.name,
          mimeType: file.type,
          sizeBytes: file.size,
        });
        processedInput.imageId = fileId;
      }
      input = processedInput;
      return service.create(input);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["products"] });
    },
  });
}

export function useUpdateProducts() {
  const service = useEngineService<ProductsService>("products");
  const queryClient = useQueryClient();
  const [fileUploadState, setFileUploadState] = useState<
    Record<
      string,
      { status: "idle" | "uploading" | "pending" | "done" | "error"; progress: number }
    >
  >({});

  return useMutation({
    mutationFn: async ({ id, input: inputParam }: { id: string; input: UpdateProductsInput }) => {
      let input = inputParam;
      // Process file fields
      const fileService = getFileUploadService();
      let processedInput = { ...input };
      if ((input.imageId as any) instanceof File) {
        const fileId = createId();
        const file = input.imageId as unknown as File;
        await fileService.saveTemp(fileId, file, {
          entityType: "assets",
          fieldName: "imageId",
          filename: file.name,
          mimeType: file.type,
          sizeBytes: file.size,
        });
        processedInput.imageId = fileId;
      }
      input = processedInput;
      return service.update(id, input);
    },
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ["products"] });
      queryClient.invalidateQueries({ queryKey: ["products", variables.id] });
    },
  });
}

export function useDeleteProducts() {
  const service = useEngineService<ProductsService>("products");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (id: string) => {
      return service.delete(id);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["products"] });
    },
  });
}

// ProductVariants hooks
export interface ProductVariantsListOptions {
  search?: string;
  limit?: number;
  offset?: number;
  sortBy?: "createdAt" | "updatedAt";
  sortOrder?: "asc" | "desc";
}

export function useProductVariantss(options?: ProductVariantsListOptions) {
  const service = useEngineService<ProductVariantsService>("product_variants");

  return useQuery({
    queryKey: options ? ["product_variants", "list", options] : ["product_variants"],
    queryFn: async () => service.list(options),
  });
}

export function useProductVariants(id: string | null) {
  const service = useEngineService<ProductVariantsService>("product_variants");

  return useQuery({
    queryKey: id ? ["product_variants", id] : ["product_variants", "detail"],
    queryFn: async () => {
      if (!id) return null;
      return service.findById(id);
    },
    enabled: !!id,
  });
}

export function useCreateProductVariants() {
  const service = useEngineService<ProductVariantsService>("product_variants");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (inputParam: CreateProductVariantsInput) => {
      let input = inputParam;

      return service.create(input);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["product_variants"] });
    },
  });
}

export function useUpdateProductVariants() {
  const service = useEngineService<ProductVariantsService>("product_variants");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async ({
      id,
      input: inputParam,
    }: {
      id: string;
      input: UpdateProductVariantsInput;
    }) => {
      let input = inputParam;

      return service.update(id, input);
    },
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ["product_variants"] });
      queryClient.invalidateQueries({ queryKey: ["product_variants", variables.id] });
    },
  });
}

export function useDeleteProductVariants() {
  const service = useEngineService<ProductVariantsService>("product_variants");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (id: string) => {
      return service.delete(id);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["product_variants"] });
    },
  });
}

// Suppliers hooks
export interface SuppliersListOptions {
  search?: string;
  limit?: number;
  offset?: number;
  sortBy?: "createdAt" | "updatedAt";
  sortOrder?: "asc" | "desc";
}

export function useSupplierss(options?: SuppliersListOptions) {
  const service = useEngineService<SuppliersService>("suppliers");

  return useQuery({
    queryKey: options ? ["suppliers", "list", options] : ["suppliers"],
    queryFn: async () => service.list(options),
  });
}

export function useSuppliers(id: string | null) {
  const service = useEngineService<SuppliersService>("suppliers");

  return useQuery({
    queryKey: id ? ["suppliers", id] : ["suppliers", "detail"],
    queryFn: async () => {
      if (!id) return null;
      return service.findById(id);
    },
    enabled: !!id,
  });
}

export function useCreateSuppliers() {
  const service = useEngineService<SuppliersService>("suppliers");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (inputParam: CreateSuppliersInput) => {
      let input = inputParam;

      return service.create(input);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["suppliers"] });
    },
  });
}

export function useUpdateSuppliers() {
  const service = useEngineService<SuppliersService>("suppliers");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async ({ id, input: inputParam }: { id: string; input: UpdateSuppliersInput }) => {
      let input = inputParam;

      return service.update(id, input);
    },
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ["suppliers"] });
      queryClient.invalidateQueries({ queryKey: ["suppliers", variables.id] });
    },
  });
}

export function useDeleteSuppliers() {
  const service = useEngineService<SuppliersService>("suppliers");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (id: string) => {
      return service.delete(id);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["suppliers"] });
    },
  });
}

// Tags hooks
export interface TagsListOptions {
  search?: string;
  limit?: number;
  offset?: number;
  sortBy?: "createdAt" | "updatedAt";
  sortOrder?: "asc" | "desc";
}

export function useTagss(options?: TagsListOptions) {
  const service = useEngineService<TagsService>("tags");

  return useQuery({
    queryKey: options ? ["tags", "list", options] : ["tags"],
    queryFn: async () => service.list(options),
  });
}

export function useTags(id: string | null) {
  const service = useEngineService<TagsService>("tags");

  return useQuery({
    queryKey: id ? ["tags", id] : ["tags", "detail"],
    queryFn: async () => {
      if (!id) return null;
      return service.findById(id);
    },
    enabled: !!id,
  });
}

export function useCreateTags() {
  const service = useEngineService<TagsService>("tags");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (inputParam: CreateTagsInput) => {
      let input = inputParam;

      return service.create(input);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["tags"] });
    },
  });
}

export function useUpdateTags() {
  const service = useEngineService<TagsService>("tags");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async ({ id, input: inputParam }: { id: string; input: UpdateTagsInput }) => {
      let input = inputParam;

      return service.update(id, input);
    },
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ["tags"] });
      queryClient.invalidateQueries({ queryKey: ["tags", variables.id] });
    },
  });
}

export function useDeleteTags() {
  const service = useEngineService<TagsService>("tags");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (id: string) => {
      return service.delete(id);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["tags"] });
    },
  });
}

// CustomerGroups hooks
export interface CustomerGroupsListOptions {
  search?: string;
  limit?: number;
  offset?: number;
  sortBy?: "createdAt" | "updatedAt";
  sortOrder?: "asc" | "desc";
}

export function useCustomerGroupss(options?: CustomerGroupsListOptions) {
  const service = useEngineService<CustomerGroupsService>("customer_groups");

  return useQuery({
    queryKey: options ? ["customer_groups", "list", options] : ["customer_groups"],
    queryFn: async () => service.list(options),
  });
}

export function useCustomerGroups(id: string | null) {
  const service = useEngineService<CustomerGroupsService>("customer_groups");

  return useQuery({
    queryKey: id ? ["customer_groups", id] : ["customer_groups", "detail"],
    queryFn: async () => {
      if (!id) return null;
      return service.findById(id);
    },
    enabled: !!id,
  });
}

export function useCreateCustomerGroups() {
  const service = useEngineService<CustomerGroupsService>("customer_groups");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (inputParam: CreateCustomerGroupsInput) => {
      let input = inputParam;

      return service.create(input);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["customer_groups"] });
    },
  });
}

export function useUpdateCustomerGroups() {
  const service = useEngineService<CustomerGroupsService>("customer_groups");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async ({
      id,
      input: inputParam,
    }: {
      id: string;
      input: UpdateCustomerGroupsInput;
    }) => {
      let input = inputParam;

      return service.update(id, input);
    },
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ["customer_groups"] });
      queryClient.invalidateQueries({ queryKey: ["customer_groups", variables.id] });
    },
  });
}

export function useDeleteCustomerGroups() {
  const service = useEngineService<CustomerGroupsService>("customer_groups");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (id: string) => {
      return service.delete(id);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["customer_groups"] });
    },
  });
}

// Visitas hooks
export interface VisitasListOptions {
  search?: string;
  limit?: number;
  offset?: number;
  sortBy?: "createdAt" | "updatedAt";
  sortOrder?: "asc" | "desc";
}

export function useVisitass(options?: VisitasListOptions) {
  const service = useEngineService<VisitasService>("visitas");

  return useQuery({
    queryKey: options ? ["visitas", "list", options] : ["visitas"],
    queryFn: async () => service.list(options),
  });
}

export function useVisitas(id: string | null) {
  const service = useEngineService<VisitasService>("visitas");

  return useQuery({
    queryKey: id ? ["visitas", id] : ["visitas", "detail"],
    queryFn: async () => {
      if (!id) return null;
      return service.findById(id);
    },
    enabled: !!id,
  });
}

export function useCreateVisitas() {
  const service = useEngineService<VisitasService>("visitas");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (inputParam: CreateVisitasInput) => {
      let input = inputParam;

      return service.create(input);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["visitas"] });
    },
  });
}

export function useUpdateVisitas() {
  const service = useEngineService<VisitasService>("visitas");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async ({ id, input: inputParam }: { id: string; input: UpdateVisitasInput }) => {
      let input = inputParam;

      return service.update(id, input);
    },
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ["visitas"] });
      queryClient.invalidateQueries({ queryKey: ["visitas", variables.id] });
    },
  });
}

export function useDeleteVisitas() {
  const service = useEngineService<VisitasService>("visitas");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (id: string) => {
      return service.delete(id);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["visitas"] });
    },
  });
}

// Sales hooks
export interface SalesListOptions {
  search?: string;
  limit?: number;
  offset?: number;
  sortBy?:
    | "saleDate"
    | "deliveryDate"
    | "orderDate"
    | "cancelledAt"
    | "refundDate"
    | "createdAt"
    | "updatedAt";
  sortOrder?: "asc" | "desc";
}

export function useSaless(options?: SalesListOptions) {
  const service = useEngineService<SalesService>("sales");

  return useQuery({
    queryKey: options ? ["sales", "list", options] : ["sales"],
    queryFn: async () => service.list(options),
  });
}

export function useSales(id: string | null) {
  const service = useEngineService<SalesService>("sales");

  return useQuery({
    queryKey: id ? ["sales", id] : ["sales", "detail"],
    queryFn: async () => {
      if (!id) return null;
      return service.findById(id);
    },
    enabled: !!id,
  });
}

export function useCreateSales() {
  const service = useEngineService<SalesService>("sales");
  const queryClient = useQueryClient();
  const [fileUploadState, setFileUploadState] = useState<
    Record<
      string,
      { status: "idle" | "uploading" | "pending" | "done" | "error"; progress: number }
    >
  >({});

  return useMutation({
    mutationFn: async (inputParam: CreateSalesInput) => {
      let input = inputParam;
      // Process file fields
      const fileService = getFileUploadService();
      let processedInput = { ...input };
      if ((input.advanceProofImageId as any) instanceof File) {
        const fileId = createId();
        const file = input.advanceProofImageId as unknown as File;
        await fileService.saveTemp(fileId, file, {
          entityType: "files",
          fieldName: "advanceProofImageId",
          filename: file.name,
          mimeType: file.type,
          sizeBytes: file.size,
        });
        processedInput.advanceProofImageId = fileId;
      }
      input = processedInput;
      return service.create(input);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["sales"] });
    },
  });
}

export function useUpdateSales() {
  const service = useEngineService<SalesService>("sales");
  const queryClient = useQueryClient();
  const [fileUploadState, setFileUploadState] = useState<
    Record<
      string,
      { status: "idle" | "uploading" | "pending" | "done" | "error"; progress: number }
    >
  >({});

  return useMutation({
    mutationFn: async ({ id, input: inputParam }: { id: string; input: UpdateSalesInput }) => {
      let input = inputParam;
      // Process file fields
      const fileService = getFileUploadService();
      let processedInput = { ...input };
      if ((input.advanceProofImageId as any) instanceof File) {
        const fileId = createId();
        const file = input.advanceProofImageId as unknown as File;
        await fileService.saveTemp(fileId, file, {
          entityType: "files",
          fieldName: "advanceProofImageId",
          filename: file.name,
          mimeType: file.type,
          sizeBytes: file.size,
        });
        processedInput.advanceProofImageId = fileId;
      }
      input = processedInput;
      return service.update(id, input);
    },
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ["sales"] });
      queryClient.invalidateQueries({ queryKey: ["sales", variables.id] });
    },
  });
}

export function useDeleteSales() {
  const service = useEngineService<SalesService>("sales");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (id: string) => {
      return service.delete(id);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["sales"] });
    },
  });
}

// Purchases hooks
export interface PurchasesListOptions {
  search?: string;
  limit?: number;
  offset?: number;
  sortBy?: "purchaseDate" | "createdAt" | "updatedAt";
  sortOrder?: "asc" | "desc";
}

export function usePurchasess(options?: PurchasesListOptions) {
  const service = useEngineService<PurchasesService>("purchases");

  return useQuery({
    queryKey: options ? ["purchases", "list", options] : ["purchases"],
    queryFn: async () => service.list(options),
  });
}

export function usePurchases(id: string | null) {
  const service = useEngineService<PurchasesService>("purchases");

  return useQuery({
    queryKey: id ? ["purchases", id] : ["purchases", "detail"],
    queryFn: async () => {
      if (!id) return null;
      return service.findById(id);
    },
    enabled: !!id,
  });
}

export function useCreatePurchases() {
  const service = useEngineService<PurchasesService>("purchases");
  const queryClient = useQueryClient();
  const [fileUploadState, setFileUploadState] = useState<
    Record<
      string,
      { status: "idle" | "uploading" | "pending" | "done" | "error"; progress: number }
    >
  >({});

  return useMutation({
    mutationFn: async (inputParam: CreatePurchasesInput) => {
      let input = inputParam;
      // Process file fields
      const fileService = getFileUploadService();
      let processedInput = { ...input };
      if ((input.receiptImageId as any) instanceof File) {
        const fileId = createId();
        const file = input.receiptImageId as unknown as File;
        await fileService.saveTemp(fileId, file, {
          entityType: "files",
          fieldName: "receiptImageId",
          filename: file.name,
          mimeType: file.type,
          sizeBytes: file.size,
        });
        processedInput.receiptImageId = fileId;
      }
      input = processedInput;
      return service.create(input);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["purchases"] });
    },
  });
}

export function useUpdatePurchases() {
  const service = useEngineService<PurchasesService>("purchases");
  const queryClient = useQueryClient();
  const [fileUploadState, setFileUploadState] = useState<
    Record<
      string,
      { status: "idle" | "uploading" | "pending" | "done" | "error"; progress: number }
    >
  >({});

  return useMutation({
    mutationFn: async ({ id, input: inputParam }: { id: string; input: UpdatePurchasesInput }) => {
      let input = inputParam;
      // Process file fields
      const fileService = getFileUploadService();
      let processedInput = { ...input };
      if ((input.receiptImageId as any) instanceof File) {
        const fileId = createId();
        const file = input.receiptImageId as unknown as File;
        await fileService.saveTemp(fileId, file, {
          entityType: "files",
          fieldName: "receiptImageId",
          filename: file.name,
          mimeType: file.type,
          sizeBytes: file.size,
        });
        processedInput.receiptImageId = fileId;
      }
      input = processedInput;
      return service.update(id, input);
    },
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ["purchases"] });
      queryClient.invalidateQueries({ queryKey: ["purchases", variables.id] });
    },
  });
}

export function useDeletePurchases() {
  const service = useEngineService<PurchasesService>("purchases");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (id: string) => {
      return service.delete(id);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["purchases"] });
    },
  });
}

// Distribuciones hooks
export interface DistribucionesListOptions {
  search?: string;
  limit?: number;
  offset?: number;
  sortBy?: "fecha" | "closedAt" | "createdAt" | "updatedAt";
  sortOrder?: "asc" | "desc";
}

export function useDistribucioness(options?: DistribucionesListOptions) {
  const service = useEngineService<DistribucionesService>("distribuciones");

  return useQuery({
    queryKey: options ? ["distribuciones", "list", options] : ["distribuciones"],
    queryFn: async () => service.list(options),
  });
}

export function useDistribuciones(id: string | null) {
  const service = useEngineService<DistribucionesService>("distribuciones");

  return useQuery({
    queryKey: id ? ["distribuciones", id] : ["distribuciones", "detail"],
    queryFn: async () => {
      if (!id) return null;
      return service.findById(id);
    },
    enabled: !!id,
  });
}

export function useCreateDistribuciones() {
  const service = useEngineService<DistribucionesService>("distribuciones");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (inputParam: CreateDistribucionesInput) => {
      let input = inputParam;

      return service.create(input);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["distribuciones"] });
    },
  });
}

export function useUpdateDistribuciones() {
  const service = useEngineService<DistribucionesService>("distribuciones");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async ({
      id,
      input: inputParam,
    }: {
      id: string;
      input: UpdateDistribucionesInput;
    }) => {
      let input = inputParam;

      return service.update(id, input);
    },
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ["distribuciones"] });
      queryClient.invalidateQueries({ queryKey: ["distribuciones", variables.id] });
    },
  });
}

export function useDeleteDistribuciones() {
  const service = useEngineService<DistribucionesService>("distribuciones");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (id: string) => {
      return service.delete(id);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["distribuciones"] });
    },
  });
}

// Abonos hooks
export interface AbonosListOptions {
  search?: string;
  limit?: number;
  offset?: number;
  sortBy?: "createdAt" | "updatedAt";
  sortOrder?: "asc" | "desc";
}

export function useAbonoss(options?: AbonosListOptions) {
  const service = useEngineService<AbonosService>("abonos");

  return useQuery({
    queryKey: options ? ["abonos", "list", options] : ["abonos"],
    queryFn: async () => service.list(options),
  });
}

export function useAbonos(id: string | null) {
  const service = useEngineService<AbonosService>("abonos");

  return useQuery({
    queryKey: id ? ["abonos", id] : ["abonos", "detail"],
    queryFn: async () => {
      if (!id) return null;
      return service.findById(id);
    },
    enabled: !!id,
  });
}

export function useCreateAbonos() {
  const service = useEngineService<AbonosService>("abonos");
  const queryClient = useQueryClient();
  const [fileUploadState, setFileUploadState] = useState<
    Record<
      string,
      { status: "idle" | "uploading" | "pending" | "done" | "error"; progress: number }
    >
  >({});

  return useMutation({
    mutationFn: async (inputParam: CreateAbonosInput) => {
      let input = inputParam;
      // Process file fields
      const fileService = getFileUploadService();
      let processedInput = { ...input };
      if ((input.proofImageId as any) instanceof File) {
        const fileId = createId();
        const file = input.proofImageId as unknown as File;
        await fileService.saveTemp(fileId, file, {
          entityType: "files",
          fieldName: "proofImageId",
          filename: file.name,
          mimeType: file.type,
          sizeBytes: file.size,
        });
        processedInput.proofImageId = fileId;
      }
      input = processedInput;
      return service.create(input);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["abonos"] });
    },
  });
}

export function useUpdateAbonos() {
  const service = useEngineService<AbonosService>("abonos");
  const queryClient = useQueryClient();
  const [fileUploadState, setFileUploadState] = useState<
    Record<
      string,
      { status: "idle" | "uploading" | "pending" | "done" | "error"; progress: number }
    >
  >({});

  return useMutation({
    mutationFn: async ({ id, input: inputParam }: { id: string; input: UpdateAbonosInput }) => {
      let input = inputParam;
      // Process file fields
      const fileService = getFileUploadService();
      let processedInput = { ...input };
      if ((input.proofImageId as any) instanceof File) {
        const fileId = createId();
        const file = input.proofImageId as unknown as File;
        await fileService.saveTemp(fileId, file, {
          entityType: "files",
          fieldName: "proofImageId",
          filename: file.name,
          mimeType: file.type,
          sizeBytes: file.size,
        });
        processedInput.proofImageId = fileId;
      }
      input = processedInput;
      return service.update(id, input);
    },
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ["abonos"] });
      queryClient.invalidateQueries({ queryKey: ["abonos", variables.id] });
    },
  });
}

export function useDeleteAbonos() {
  const service = useEngineService<AbonosService>("abonos");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (id: string) => {
      return service.delete(id);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["abonos"] });
    },
  });
}

// Files hooks
export interface FilesListOptions {
  search?: string;
  limit?: number;
  offset?: number;
  sortBy?: "createdAt" | "deletedAt" | "updatedAt";
  sortOrder?: "asc" | "desc";
}

export function useFiless(options?: FilesListOptions) {
  const service = useEngineService<FilesService>("files");

  return useQuery({
    queryKey: options ? ["files", "list", options] : ["files"],
    queryFn: async () => service.list(options),
  });
}

export function useFiles(id: string | null) {
  const service = useEngineService<FilesService>("files");

  return useQuery({
    queryKey: id ? ["files", id] : ["files", "detail"],
    queryFn: async () => {
      if (!id) return null;
      return service.findById(id);
    },
    enabled: !!id,
  });
}

export function useCreateFiles() {
  const service = useEngineService<FilesService>("files");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (inputParam: CreateFilesInput) => {
      let input = inputParam;

      return service.create(input);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["files"] });
    },
  });
}

export function useUpdateFiles() {
  const service = useEngineService<FilesService>("files");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async ({ id, input: inputParam }: { id: string; input: UpdateFilesInput }) => {
      let input = inputParam;

      return service.update(id, input);
    },
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ["files"] });
      queryClient.invalidateQueries({ queryKey: ["files", variables.id] });
    },
  });
}

export function useDeleteFiles() {
  const service = useEngineService<FilesService>("files");
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (id: string) => {
      return service.delete(id);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["files"] });
    },
  });
}
