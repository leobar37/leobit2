# Set to "1" to force synchronous (non-worker) PGlite mode.
# By default, PGlite runs in a Web Worker for better main-thread performance.
# Disable only if you encounter worker-related issues in your browser.
VITE_DISABLE_PGLITE_WORKER=
