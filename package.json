{
  "name": "avileo",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.1.38",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "db:migrate": "turbo run db:migrate",
    "db:generate": "turbo run db:generate",
    "db:backup": "cd packages/backend && bun run db:backup",
    "test:e2e": "./scripts/test-e2e.sh"
  },
  "devDependencies": {
    "turbo": "^2.7.2",
    "typescript": "^5.3.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@radix-ui/react-dialog": "^1.1.15",
    "@radix-ui/react-slot": "^1.2.4",
    "bun-types": "^1.3.11"
  }
}
