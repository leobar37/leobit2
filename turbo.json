{
  "$schema": "https://turbo.build/schema.json",
  "ui": "stream",
  "remoteCache": {
    "enabled": true,
    "timeout": 30
  },
  "globalDependencies": ["package.json", "bun.lock", "drizzle-sync.config.ts"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**"],
      "cache": true
    },
    "@avileo/app#build": {
      "dependsOn": ["^build", "@avileo/shared#build"],
      "outputs": ["build/**"]
    },
    "@avileo/backend#build": {
      "dependsOn": ["^build", "@avileo/shared#build"],
      "outputs": ["dist/**"]
    },
    "@avileo/shared#build": {
      "outputs": ["dist/**"]
    },
    "@avileo/drizzle-sync#build": {
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "db:migrate": {
      "cache": false
    },
    "db:generate": {
      "cache": false
    }
  }
}
