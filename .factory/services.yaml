commands:
  install: bun install
  build: bun run build
  typecheck: cd packages/app && bun run typecheck
  test:app: cd packages/app && bun run test --run
  test:backend: cd packages/backend && bun run test --run
  lint: echo "No lint command configured"

services: {}
